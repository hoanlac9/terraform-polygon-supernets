apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
{% for item in groups['monitoring_001'] %}
    url: http://{{ hostvars[item].private_ip_address }}:9090
{% endfor %}
